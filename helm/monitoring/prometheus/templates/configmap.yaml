apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
data:
  prometheus.yml: |
    {{ toYaml (index .Values.serverFiles "prometheus.yml") | indent 4 }}